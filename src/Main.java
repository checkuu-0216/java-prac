import java.util.Arrays;

public class Main {
    public static void main(String[] args) {
        // 우리의 공간
        // boolean 변수 선언
        //타입이름 = 값;

// 참조형 배열
        int[] a ={1, 2, 3};
//참조형 데이터는 주소 값을 대입한다.
        System.out.println(Arrays.toString(a));
    }
}